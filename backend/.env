# Server port (default: 5000)
PORT=5000

# MongoDB Atlas connection string
# Replace <username>, <password>, and <cluster> with your Atlas credentials
MONGODB_URI=mongodb+srv://pranayjha952_db_user:@cluster0.rtelvzl.mongodb.net/?appName=Cluster0

# Secret key used to sign and verify JWT tokens (use a long random string in production)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Token expiration time (e.g. 7d, 24h, 60m)
JWT_EXPIRES_IN=7d
  